% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkagg.R
\name{linkagg}
\alias{linkagg}
\title{Start a linked figure}
\usage{
linkagg(
  data,
  key,
  threads = TRUE,
  thread_cap = 160L,
  points = c("auto", "svg", "canvas"),
  canvas_threshold = 6000L,
  palette = NULL
)
}
\arguments{
\item{data}{A data frame with one row per unit of selection. For clinical
safety use this is a subject-level dataset such as ADSL, one row per
subject, restricted to the analysis population.}

\item{key}{Column uniquely identifying each row, such as \code{USUBJID}. Bare
name or string.}

\item{threads}{Draw animated threads from selected rows to the groups they
belong to. This is what makes the row-to-aggregate mapping visible. Set
\code{FALSE} for a quieter figure.}

\item{thread_cap}{Maximum threads drawn. Above this, threads are sampled
evenly so the display stays legible.}

\item{points}{Renderer for the row-level display: \code{"auto"} switches to
canvas above \code{canvas_threshold} rows, \code{"svg"} and \code{"canvas"} force one.}

\item{canvas_threshold}{Row count above which \code{"auto"} uses canvas.}

\item{palette}{Named list of colours overriding the default light theme.
Recognised names: \code{ground}, \code{panel}, \code{data}, \code{select}, \code{zone}, \code{text},
\code{dim}, \code{rule}, \code{mute} for marks outside the selection, and \code{arms} for a
vector of one colour per treatment arm. Arm colours default to an
Okabe-Ito derived set that survives colour blindness and greyscale
printing, with a neutral first colour for placebo.}
}
\value{
An object of class \code{linkagg_spec}.
}
\description{
Begins a specification. Add displays with \code{\link[=view_points]{view_points()}}, \code{\link[=view_bars]{view_bars()}} and
\code{\link[=view_table]{view_table()}}, then render with \code{\link[=as_linkagg_widget]{as_linkagg_widget()}}.
}
\examples{
set.seed(1)
adsl <- data.frame(
  USUBJID = sprintf("01-\%03d", 1:120),
  ARM     = rep(c("Placebo", "Drug A 50mg", "Drug A 100mg"), each = 40),
  ALT     = exp(rnorm(120)),
  TBILI   = exp(rnorm(120))
)
adsl$SOC <- replicate(120, sample(c("Hepatobiliary disorders",
                                    "Investigations",
                                    "Gastrointestinal disorders"),
                                  sample(0:3, 1)), simplify = FALSE)

linkagg(adsl, USUBJID) |>
  view_points(TBILI, ALT, log_x = TRUE, log_y = TRUE) |>
  view_bars(SOC, by = ARM) |>
  view_table(cols = c("USUBJID", "ARM", "ALT", "TBILI"))

}
