% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkagg.R
\name{view_bars}
\alias{view_bars}
\title{Add an aggregate bar display, optionally split by treatment arm}
\usage{
view_bars(
  spec,
  group,
  by = NULL,
  drill = NULL,
  group_levels = NULL,
  by_levels = NULL,
  denominator = c("population", "count"),
  population = NULL,
  label = NULL
)
}
\arguments{
\item{spec}{A \code{linkagg_spec}.}

\item{group}{Grouping column. Either an ordinary column with one value per
row, or a list-column where each element is a character vector, for rows
in several groups at once, such as a subject with events in several system
organ classes.}

\item{by}{Optional column giving the treatment arm, one value per row.}

\item{drill}{Optional finer term to drill into, such as preferred term
below system organ class. Must be shaped like \code{group}: if \code{group} is a
list-column, \code{drill} must be a list-column whose elements pair up
positionally, so element \code{j} of \code{drill[[i]]} is the term for element \code{j}
of \code{group[[i]]}.}

\item{group_levels}{Optional character vector fixing the order of groups.
Defaults to descending overall count.}

\item{by_levels}{Optional character vector fixing the order of arms.
Defaults to the factor levels, or sorted unique values.}

\item{denominator}{\code{"population"} gives percentages of each arm's analysis
population; \code{"count"} gives raw counts. Defaults to \code{"population"} when
\code{by} is supplied and \code{"count"} otherwise.}

\item{population}{Denominator source. \code{NULL} counts rows per arm in \code{data},
which is correct when \code{data} is the analysis population. Otherwise pass a
named numeric vector such as \code{c(Placebo = 80, "Drug A" = 82)}, or the full
population data frame to count from.}

\item{label}{Display title. Defaults to the column name.}
}
\value{
The updated \code{linkagg_spec}.
}
\description{
One bar per group, each standing for many rows. When a selection is active
each bar fills to the share of its own rows that are selected, which is the
behaviour existing linked-brushing tools in R do not provide.
}
\details{
Supply \code{by} to split every group by treatment arm, which is how safety
displays are actually read. With \code{by} set, bar length is the percentage of
that arm's analysis population, matching the denominator convention of a
standard adverse event summary. Without \code{by}, bar length is a raw count.
}
