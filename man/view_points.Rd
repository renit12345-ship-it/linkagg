% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linkagg.R
\name{view_points}
\alias{view_points}
\title{Add a row-level scatter display}
\usage{
view_points(
  spec,
  x,
  y,
  log_x = FALSE,
  log_y = FALSE,
  x_lab = NULL,
  y_lab = NULL,
  zone = NULL,
  facet = NULL,
  facet_levels = NULL,
  facet_row = NULL,
  facet_row_levels = NULL
)
}
\arguments{
\item{spec}{A \code{linkagg_spec}.}

\item{x, y}{Numeric columns. Bare names or strings.}

\item{log_x, log_y}{Use a log scale on that axis.}

\item{x_lab, y_lab}{Axis labels. Default to the column names.}

\item{zone}{Optional threshold region drawn behind the data, as a list with
\code{x} and \code{y} numeric cut-points and an optional \code{label}. Marks above both
cut-points fall inside the region.}

\item{facet}{Optional column giving one small multiple per level, drawn on
shared scales so panels are comparable. Brushing acts within one panel.
With \code{facet_row} also set, this becomes the column variable of a grid.}

\item{facet_levels, facet_row_levels}{Optional character vectors fixing the
order of columns and of rows.}

\item{facet_row}{Optional second column, laid out down the rows to give a
full grid of panels: one column per level of \code{facet}, one row per level
of \code{facet_row}, such as arm across and sex down. Scales stay shared across
the whole grid, so every panel is comparable with every other, and a brush
still selects only the rows in the one cell you dragged over.}
}
\value{
The updated \code{linkagg_spec}.
}
\description{
One mark per row, and the display you brush to make a selection. For liver
safety this is the eDISH shape: peak ALT against peak total bilirubin on log
scales, with a reference region.
}
